# Terraform
.terraform/
terraform.tfstate
terraform.tfstate.backup
*.tfplan
tfplan
terraform.tfvars
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Dashboard config (contains SSH key path — keep local)
dashboard/config.yaml

# macOS
.DS_Store
